daemon off;
error_log logs/error.log debug;
pid logs/nginx.pid;

events { worker_connections 16; }

http {
    server {
        listen 8888;
        location / {
            llm_security;
            llm_security_mode detect;
            return 200 "ok";
        }
    }
}
