daemon off;
error_log logs/error.log debug;
pid logs/nginx.pid;

events {
    worker_connections 16;
}

http {
    llm_metrics_zone first 1m;
    llm_metrics_zone second 1m;

    server {
        listen 8888;
        location /metrics {
            llm_metrics_export prometheus;
        }
    }
}
