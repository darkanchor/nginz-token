[test]
preload = ["./tests/preload.js"]
timeout = 10000
# The harness keeps a process-global port plan / TEST_URL (configureTestPorts).
# Parallel files stomp that state and also race nginx listen ports / Bun mocks.
# Serialize so each suite owns the plan for the duration of its describes.
maxConcurrency = 1
